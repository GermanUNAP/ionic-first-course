import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'platzi-music-tabs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
